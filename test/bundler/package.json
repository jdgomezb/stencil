{
  "private": true,
  "scripts": {
    "clean": "",
    "build": "npm run build.component-lib && npm run build.parcel && npm run build.vite",
    "build.component-lib": "cd component-library && npm ci && npm run build && cd ..",
    "build.parcel": "cd parcel-bundle-test && npm ci && npm run build && cd ..",
    "build.vite": "cd vite-bundle-test && npm ci && npm run build && cd ..",
    "karma": "karma start karma.config.js"
  },
  "devDependencies": {
    "@types/node": "^14.18.12",
    "bootstrap": "^4.5.1",
    "karma": "^6.3.17",
    "karma-chrome-launcher": "^3.1.0",
    "karma-jasmine": "^5.0.0",
    "karma-polyfill": "^1.1.0",
    "karma-typescript": "^5.5.3"
  },
  "volta": {
    "node": "12.22.7",
    "npm": "6.14.15"
  }
}
